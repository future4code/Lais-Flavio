
//A) Com o process.argv, rodamos o comando tsc && node build/pasta/arquivo.js e parâmetros

/*B)

export {}
const name: string = process.argv[2];
const age: number = Number(process.argv[3]);

console.log(`Olá! Você, ${name}, tem ${age} anos`);

*/

/*C)
export {}

const name: string = process.argv[2];
const age: number = Number(process.argv[3]);
const newAge: number = Number(age + 7)

console.log(`Olá! Você, ${name}, tem ${age} anos. Em sete anos você terá ${newAge}`);
*/
